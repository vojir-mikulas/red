/target
/build/
**/*.rs.bk
*.db
*.sqlite
*.sqlite3
# …but the shipped sample-database preview is committed (regenerate from sample/sample.sql).
!sample/sample.db

# Signing material — never commit. Lives only in your keychain / GitHub Secrets.
*.p12
*.p8
*.cer
*.certSigningRequest

# Editor / OS noise
.DS_Store
*.swp
.idea/
.vscode/

/design/
/docs/plans/
/docs/codereviews/
/docs/security/
/docs/guidelines/
.claude
/docker

# Local Claude/agent settings overrides
.claude/settings.local.json
