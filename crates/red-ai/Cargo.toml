[package]
name = "red-ai"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
# Provider-agnostic conversation/tool types are expressed in plain serde-able
# structs; nothing here knows about GPUI or the database driver.
async-trait.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tracing.workspace = true
# `macros` for the `select!` that races a streamed chunk against cancellation,
# so Stop interrupts a stalled read instead of waiting for the next chunk.
tokio = { workspace = true, features = ["macros", "sync"] }
futures-util.workspace = true
reqwest.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt", "sync"] }

[lints]
workspace = true
