# The `result` catalog. Generated by scripts/i18n-extract.py; do not
# edit by hand. The locale is this file's name, the folder is the UI area.
# Every locale mirrors these messages in its own file next to this one, so
# two translators never edit the same lines.
#
# Plural forms belong in the translation, not in the code:
#
#   some-message = { $n ->
#       [one] { $n } row
#      *[other] { $n } rows
#   }

result-pin_limit = Six pinned rows is the maximum; unpin one to pin another

result-unpin_row = Unpin row

result-empty_hint = Double-click a table or run a query to see rows

result-query_failed = Query failed

result-filter_rows_tip = Filter rows (⌘⇧F)

result-clear_filter = Clear filter

result-rows_unit = rows

result-stats_pick_column = Select a column to summarize

result-stats_computing = computing…

result-stats_unavailable = stats unavailable

result-stats_distinct = distinct

result-column_computed = computed

result-column_default = default

result-pin_column_too_wide = Frozen columns can take at most half the grid; narrow one or unpin another first

result-loading = Loading…

result-watch_fully_refreshed = fully refreshed
