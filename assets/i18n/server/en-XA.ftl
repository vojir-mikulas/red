# The `server` catalog. Generated by scripts/i18n-extract.py; do not
# edit by hand. The locale is this file's name, the folder is the UI area.
# Every locale mirrors these messages in its own file next to this one, so
# two translators never edit the same lines.
#
# Plural forms belong in the translation, not in the code:
#
#   some-message = { $n ->
#       [one] { $n } row
#      *[other] { $n } rows
#   }

server-mutations_title = [Mútátíöñs]

server-title = [Sérvér]

server-sampled_at = [ás öf { $when }]

server-not_visible = [Ñöt vísíblé tö thís çöññéçtíöñ:]

server-redis_monitor_hint = [Slöw lög, MÖÑÍTÖR áñd pér-çlíéñt détáíl áré íñ thé Möñítör táb.]

server-sessions_blocking = [{ $blocking_count } séssíöñ(s) blöçkíñg { $blocked } öthér(s)]

server-this_connection = [thís çöññéçtíöñ]
